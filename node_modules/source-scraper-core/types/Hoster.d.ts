export interface IHoster {
    name: string;
    quality?: string | number;
    url: string;
}
export declare class Hoster implements IHoster {
    name: string;
    quality?: string | number;
    url: string;
    constructor({ url, name, quality }: IHoster);
}
